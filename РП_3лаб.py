import math

f = open("/Users/irayangirova/Downloads/РП_1лаба.txt")
z = []
for line in f:

    numbers = line.strip().split()
    if len(numbers) > 0:
        row = []
        for x in numbers:
            x = x.replace(',', '.')
            row.append(float(x))
        z.append(row)
f.close()


N = len(z)
p = len(z[0])
dig = 4

print('\nСреднее значение j-го признака:')
z_ = []
for j in range(p):
    su = 0
    for i in range(N): su += z[i][j]
    su /= N
    z_.append(su)


print('\n\nОценка дисперсии j-го столбца:')
s2 = []
for j in range(p):
    su = 0
    for i in range(N): su += (z[i][j] - z_[j])*(z[i][j] - z_[j])
    su /= N
    s2.append(su)


print('\n\nОтклонение j-го столбца:')
s = []
for j in range(p):
    su = s2[j]**0.5
    s.append(su)


print('\n\nСтандартизованная матрица Х(N*p):')
x = [[] for i in range(N)]
for i in range(N):
    for j in range(p):
        x[i].append((z[i][j] - z_[j]) / s[j])
        print(round(x[i][j], dig), end=' ')
    print()

print('\n\nКорреляционная матрица R(p*p):')
r = [[] for i in range(p)]
for i in range(p):
    for j in range(p):
        su = 0
        for k in range(N): su += x[k][i]*x[k][j]
        su /= N
        r[i].append(su)
        print(round(r[i][j], dig), end=' ')
    print()
print()


corr_matrix = [
    [1.0, 0.2514, 0.3807, 0.0298, 0.0782, 0.0527, 0.319, -0.0217, 0.2127, 0.2621],
    [0.2514, 1.0, 0.733, 0.3363, -0.2313, -0.1378, -0.0078, -0.2572, 0.1877, 0.4579],
    [0.3807, 0.733, 1.0, 0.3518, -0.4011, -0.3614, -0.1084, -0.3932, 0.0753, 0.3277],
    [0.0298, 0.3363, 0.3518, 1.0, -0.301, -0.187, 0.009, -0.2447, 0.0396, 0.0932],
    [0.0782, -0.2313, -0.4011, -0.301, 1.0, 0.6691, 0.4611, 0.7957, 0.2517, 0.1162],
    [0.0527, -0.1378, -0.3614, -0.187, 0.6691, 1.0, 0.5555, 0.6359, 0.3729, 0.2718],
    [0.319, -0.0078, -0.1084, 0.009, 0.4611, 0.5555, 1.0, 0.4832, 0.3546, 0.4946],
    [-0.0217, -0.2572, -0.3932, -0.2447, 0.7957, 0.6359, 0.4832, 1.0, 0.4371, 0.231],
    [0.2127, 0.1877, 0.0753, 0.0396, 0.2517, 0.3729, 0.3546, 0.4371, 1.0, 0.4975],
    [0.2621, 0.4579, 0.3277, 0.0932, 0.1162, 0.2718, 0.4946, 0.231, 0.4975, 1.0]
]

""" Проверка на многомерное нормальное распределение (сумма всех недиагональнгых элемнтов в квадратн
умноженное на N (статистика подчиняется хиквадрат распределению с p(p-1)/2 с
степенями свободы

кор. матрица не отличается от единичный матрицы d<=Xi^2   с певелью
"""


d = 0

for i in range(p):
    for j in range(p):
        if (i != j):
            d += (corr_matrix[i][j])**2
d = d * N
print(f"d = {d}")


df = p * (p - 1) // 2

print(f"\ndf = {df}")

print(f"\nСРАВНЕНИЕ:")
print(f"d = {d:.2f}")
print(f"Критическое значение χ²(0.05) для df={df} ≈ 61.66")
print(f"d >> критического значения")
print("Матрица отлична от единичной\n\n")



"""Метод Якоби"""
"""Применяется для отыскания всех собсвенных значений и собственных векторов сим. матрицы
ИЗ А получается некоторая диагональная матрица Ak = T^T*A*Т, имеющая те же
собственные значения, что и А, в то врем собственные значения диагональной матрицы совпадают 
с ее диагональными элементами"""


"""Входные данные"""
"""размер матрицы"""
n = p
"""Точность"""
e = 0
"""A(n*n) - заданная матрица"""
A = corr_matrix

"""Выходные данные - А(nxn) -матрица, на диагонали которой содержатся собственные числа
T(nxn) - матрица, столбцы которой явялются собственными векторами"""

# T = E единичная матрица порядка n
T = []
for i in range(n):
    row = []
    for j in range(n):
        if  i != j:
            row.append(0.0)
        else:
            row.append(1.0)
    T.append(row)
    print(row)

#Вычисляем первую преграду

alf0 = 0
for i in range(n):
    for j in range(n):
        if i != j:
            alf0 += (A[i][j])**2

alf0 = (math.sqrt(alf0)) / n
print()
print(f"alf0 = {alf0}\n")

"""Находим наибольший по модулю внедиагональный элемент ,
превосходящий текущую преграду
Если такого элемента нет, то переходим к шагу 5 алгоритма, иначе
идем к шагу 4."""

a_pq = A[0][1]
curr=0
p_i = 0
q_j = 0

for i in range(p):
    for j in range(p):
        if (i != j):
            curr = A[i][j]
            if abs(curr) > abs(a_pq):
                a_pq = curr
                p_i = i
                q_j = j

print(f"a_pq = {a_pq}\n")
print(f"p_i = {p_i}\n")
print(f"q_j = {q_j}\n")

"""if abs(a_pq) > abs(alf0): ---------------условие для цикла"""


y = (A[p_i][p_i] - A[q_j][q_j])/2
print(f"y = {y}\n")

def sign(x):
    return 1 if x > 0 else -1 if x < 0 else 0

if y == 0:
    x = -1
else:
    x = -sign(y) * (a_pq / math.sqrt(a_pq**2 + y**2))

print(f"x = {x}\n")

inner_sqrt = math.sqrt(1 - x ** 2)
s = x / math.sqrt(2 * (1 + inner_sqrt))
print(f"\nВычисляем s = sin(θ):")
print(f"s = {s:.6f}")

c = math.sqrt(1 - s ** 2)
print(f"\nВычисляем c = cos(θ):")
print(f"{c:.6f}")

# Проверка: c² + s² = 1
print(f"\n5. Проверка: c² + s² = {c ** 2:.6f} + {s ** 2:.6f} = {c ** 2 + s ** 2:.6f} (должно быть 1)")




# ============================================
# ШАГ ПРЕОБРАЗОВАНИЯ ЯКОБИ
# ============================================
print("\n" + "=" * 70)
print("ШАГ 4: ПРЕОБРАЗОВАНИЕ МАТРИЦЫ")
print("=" * 70)

# Сохраняем исходные значения диагональных элементов для части 2
V1 = A[p_i][p_i]  # a_pp
V2 = A[p_i][q_j]  # a_pq
V3 = A[q_j][q_j]  # a_qq

# Предвычисленные значения
Z5 = s * s  # s²
Z6 = c * c  # c²
Z7 = s * c  # s·c

print(f"\nИсходные значения для элемента p={p_i + 1}, q={q_j + 1}:")
print(f"V1 (a_pp) = {V1:.6f}")
print(f"V2 (a_pq) = {V2:.6f}")
print(f"V3 (a_qq) = {V3:.6f}")
print(f"\nПредвычисленные константы:")
print(f"Z5 (s²) = {Z5:.6f}")
print(f"Z6 (c²) = {Z6:.6f}")
print(f"Z7 (s·c) = {Z7:.6f}")

# ЧАСТЬ 1: Преобразование элементов, где i ≠ p и i ≠ q
print("\n--- Часть 1: Преобразование строк i ≠ p, i ≠ q ---")
for i in range(n):
    if i != p_i and i != q_j:
        # Сохраняем исходные значения
        Z1 = A[i][p_i]  # a_ip
        Z2 = A[i][q_j]  # a_iq

        # Вычисляем новые значения
        a_ip_new = Z1 * c - Z2 * s
        a_iq_new = Z1 * s + Z2 * c

        # Обновляем матрицу (с учетом симметрии)
        A[i][p_i] = a_ip_new
        A[p_i][i] = a_ip_new
        A[i][q_j] = a_iq_new
        A[q_j][i] = a_iq_new

        print(f"i={i + 1}: a_ip было {Z1:.6f}, стало {a_ip_new:.6f}; a_iq было {Z2:.6f}, стало {a_iq_new:.6f}")

# ЧАСТЬ 2: Преобразование элементов на пересечении p и q
print("\n--- Часть 2: Преобразование диагональных элементов и a_pq ---")

# Вычисляем новые значения
a_pp_new = V1 * Z6 + V3 * Z5 - 2 * V2 * Z7
a_qq_new = V1 * Z5 + V3 * Z6 + 2 * V2 * Z7
a_pq_new = (V1 - V3) * Z7 + V2 * (Z6 - Z5)

# Обновляем матрицу
A[p_i][p_i] = a_pp_new
A[q_j][q_j] = a_qq_new
A[p_i][q_j] = a_pq_new
A[q_j][p_i] = a_pq_new  # симметрия

print(f"a_pp: {V1:.6f} → {a_pp_new:.6f}")
print(f"a_qq: {V3:.6f} → {a_qq_new:.6f}")
print(f"a_pq: {V2:.6f} → {a_pq_new:.6f} (должен быть близок к 0)")

# ЧАСТЬ 3: Преобразование матрицы собственных векторов T
print("\n--- Часть 3: Преобразование матрицы собственных векторов T ---")
for i in range(n):
    # Сохраняем исходные значения
    Z3 = T[i][p_i]  # t_ip
    Z4 = T[i][q_j]  # t_iq

    # Вычисляем новые значения
    t_ip_new = Z3 * c - Z4 * s
    t_iq_new = Z3 * s + Z4 * c

    # Обновляем матрицу T
    T[i][p_i] = t_ip_new
    T[i][q_j] = t_iq_new

    if i < 5:  # Показываем только первые 5 строк для наглядности
        print(f"i={i + 1}: t_ip было {Z3:.6f}, стало {t_ip_new:.6f}; t_iq было {Z4:.6f}, стало {t_iq_new:.6f}")

print("\n" + "=" * 70)
print("РЕЗУЛЬТАТ ПРЕОБРАЗОВАНИЯ")
print("=" * 70)

# Выводим измененные строки матрицы A
print("\nИзмененные строки матрицы A:")
for i in range(n):
    if i == p_i or i == q_j:
        row_str = f"Строка {i + 1}: "
        for j in range(min(10, n)):
            row_str += f"{A[i][j]:8.4f} "
        print(row_str)

# Проверяем, что элемент a_pq стал близок к 0
print(f"\nПРОВЕРКА: a_pq после преобразования = {A[p_i][q_j]:.10f}")
print(f"Целевое значение: 0.0")
